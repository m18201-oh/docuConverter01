# 환경 설정

uv 설치 후 이 저장소에서 `uv sync` (Python 3.12 고정). 실행은 `uv run python …`.
HWP(COM/pyhwp)는 `uv sync --extra hwp`.

---

2. unhwp (Rust 기반 - 고속)
Rust로 작성된 빠르고 효율적인 HWP/HWPX 파서입니다. 표, 이미지, 구조를 잘 유지합니다. 
저장소/패키지: crates.io - unhwp
사용법:
bash
cargo install unhwp-cli
unhwp document.hwp

---

# PDF → MD 명령어 목록

## 통합 진입점 — main.py (추천)

```powershell
uv run python main.py                    # 대화형 메뉴
uv run python main.py <번호> [입력] [출력]  # 비대화형 CLI
```

PDF 관련 시나리오 번호:

| 번호 | 내용 | 예시 |
|---|---|---|
| 1 | PDF 단일, 이미지 포함, 고품질 | `uv run python main.py 1 "input/파일.pdf" output` |
| 2 | PDF 단일, 텍스트 전용, 빠름 | `uv run python main.py 2 "input/파일.pdf" output` |
| 3 | PDF 배치, 이미지 포함, 순차 | `uv run python main.py 3 input output` |
| 4 | PDF 배치, 텍스트 전용, 빠름 | `uv run python main.py 4 input output` |
| 5 | PDF 배치, 병렬, 멀티코어 | `uv run python main.py 5 input output` |
| 10 | 이미지만 추출 | `uv run python main.py 10 input output` |
| 11 | MD 병합 | `uv run python main.py 11` |
| 12 | 이미지 경로 업데이트 | `uv run python main.py 12` |

번호 없이 `uv run python main.py` 만 치면 메뉴 뜸.

## 개별 스크립트 직접 실행

### 이미지 포함, 고품질 — convert_with_cropped_images.py

```powershell
uv run python convert_with_cropped_images.py                                   # 배치: input/ → output/
uv run python convert_with_cropped_images.py --single "input/파일.pdf" output   # 단일
```

argparse 없음. 배치는 경로 하드코딩 (input/output).

### 텍스트 전용, 빠름 — convert_pdfs_fast.py

```powershell
uv run python convert_pdfs_fast.py
uv run python convert_pdfs_fast.py --input_dir input --output_dir output --languages ko,en
```

PDF 계열 중 유일하게 OCR 언어 지정 가능.

### 병렬 — convert_pdfs_parallel.py

```powershell
uv run python convert_pdfs_parallel.py --input_dir input --output_dir output --workers 4
```

`--workers` 기본 2.

### 기본 배치 — convert_pdfs.py

```powershell
uv run python convert_pdfs.py
```

인자 없음. 전부 하드코딩.

## 후처리

```powershell
uv run python extract_images.py        # PDF → 이미지만 추출
uv run python update_image_paths.py    # MD 이미지 링크 재작성 → *_updated.md
uv run python reorganize_images.py     # 이미지 output/images/ 로 모으고 링크 수정
uv run python merge_markdown.py        # output/*.md → merged_all.md
```

넷 다 인자 없음, 경로 하드코딩.

## 주의

- `convert.py` 는 PDF 아님. HWP/HWPX/HML 전용. `uv run python convert.py --help`
- 이미지 링크 깨짐 이슈는 1·3번(= `convert_with_cropped_images.py`) 경로에만 해당. 파일명에 공백·괄호가 있으면 표준 마크다운 `![](경로)` 링크가 깨짐.
